import React from 'react';

// This component is primarily used to handle text selection functionality
// The actual selection handling is done in the main Chatbot component
const SelectionHandler = () => {
  return null;
};

export default SelectionHandler;